---
title: getType
---

import * as turf from "@turf/turf";
import ExampleMap from "@site/src/components/ExampleMap";
    
### Description

Get GeoJSON object's type, Geometry type is prioritize.

### Parameters

| Name         | Type                                                         | Description                                                                     |
| ------------ | ------------------------------------------------------------ | ------------------------------------------------------------------------------- |
| geojson      | **[GeoJSON](https://tools.ietf.org/html/rfc7946#section-3)** | GeoJSON object                                                                  |
| name<i>?</i> | **string**                                                   | name of the variable to display in error message (unused) _(default "geojson")_ |

### Returns

<ul>
  **string** GeoJSON type

</ul>

### Examples

```javascript
var point = {
  type: "Feature",
  properties: {},
  geometry: {
    type: "Point",
    coordinates: [110, 40],
  },
};
var geom = turf.getType(point);
//="Point"
```

### Installation

```javascript
$ npm install @turf/invariant

import { getType } from "@turf/invariant";
const result = getType(...);
```

```javascript
$ npm install @turf/turf

import * as turf from "@turf/turf";
const result = turf.getType(...);
```
